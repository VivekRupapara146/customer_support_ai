// Deployment configuration. Edit apiBaseUrl to point at your deployed
// backend before hosting this frontend anywhere other than localhost.
// Kept as a separate file (not baked into app.js) so this is the ONLY
// file that needs editing per-deployment.
window.TECHMART_CONFIG = {
  apiBaseUrl: "http://127.0.0.1:8000",
};
